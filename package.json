{
  "name": "transforma_tec",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "server": "nodemon server.js",
    "my-app": "npm run start --prefix my-app",
    "dev": "Concurrently \"npm run server\" \"npm run my-app\"",
    "install-packages-linux": "npm install && cd my-app && npm install && cd ..",
    "install-packages-windows": "npm install && cd my-app && npm install && cd.."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.1.3",
    "concurrently": "^7.5.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "nodemon": "^2.0.20",
    "react-router-dom": "^5.2.0"
  }
}
